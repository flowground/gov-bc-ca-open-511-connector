{
    "type": "object",
    "properties": {
        "format": {
            "required": false,
            "default": "json",
            "enum": [
                "json",
                "xml"
            ],
            "type": "string"
        }
    }
}